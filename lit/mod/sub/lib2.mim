// UNSUPPORTED: always
// RUN: %mim -p opt %s -o -
// Imported by mod.mim - not a test of its own.

// Relative to *this* file, not to whoever imports it; resolves to the same file as mod.mim's `import "lib.mim"`.
import "../lib.mim";

pub let base  = 100I32;
pub let total = lib.answer;

pub mod util {
    pub let scale = 3I32;

    pub mod deep {
        pub let leaf = 9I32;
    }
}
