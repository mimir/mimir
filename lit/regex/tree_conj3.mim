// RUN: %mim %s

plugin regex;
plugin refly;

// unlike `disj`, `conj` is ordered and keeps its duplicates
let _ = refly.equiv.struc_ne (regex.conj (regex.cls.w, regex.cls.d, regex.cls.w, regex.cls.d),
                              regex.conj (regex.cls.w, regex.cls.d));
