// RUN: %mim %s

plugin regex;
plugin refly;

let _ = refly.struc.e
    (regex.disj (regex.cls.w, regex.cls.d, regex.lit '0', regex.lit '5', regex.lit '9', regex.lit 'Z', regex.lit '_', regex.lit 'a'),
     regex.cls.w);
