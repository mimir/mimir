// RUN: rm -f %{s:stem}.ll
// RUN: %mim -p opt %s -p ll

plugin core;
plugin demo;
plugin mem;
plugin refly;

extern con main (m: mem.M 0, argc: I32, argv: mem.Ptr (mem.Ptr (I8, 0), 0), return: Cn [mem.M 0, I32]) =
    let c = refly.equiv.struc_eq (demo.const_idx i32, 42I32);
    return (m, c);
