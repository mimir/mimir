// RUN: rm -f %t.ll
// RUN: %mim -p opt -o - %s | %FileCheck %s

plugin core;
plugin matrix;

con extern f (mem: %mem.M 0, (k l: Nat), return: Cn [%mem.M 0]) =
    let (mem, M) = %buffer.constant (2, (k, l), I32) (mem, 0I32);
    return mem;

// CHECK-NOT: %matrix.
