// RUN: %mim -p opt %s -o -

axm Phase: *;

axm phase1: Bool         → Phase;
axm phase2: []            → Phase;
axm phase3: [Nat, Bool] → Phase;

rec PiPeline: * = Phase → PiPeline; // got the pun? XD

axm pipe: PiPeline;

let pipeline = pipe
    (phase1 tt)
    (phase2 ())
    (phase3 (23, tt));
