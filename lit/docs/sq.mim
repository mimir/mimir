// RUN: %mim -p opt %s -o - | %FileCheck %s
plugin core;

lam sq (T: *, mul: [T, T] → T) (x: T): T = mul (x, x);

fun extern f(x: Nat): Nat =
    return (sq (Nat, %core.nat.mul) x);

// CHECK: %core.nat.mul ‹2; x
