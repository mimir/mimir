// RUN: (! %mim -p opt %s 2>&1) | %FileCheck %s

extern rule dbl (a: Nat): (a + a) => (a * 2);

// CHECK: {{.*}}extrule.mim:3:1-3:6: error: `extern` is only meaningful on a function declaration, not a rewrite rule
