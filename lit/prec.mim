// RUN: %mim -p opt %s -o - | %FileCheck %s

extern lam f (x: Nat): Nat = x;
extern lam higher(f: (Nat → Nat) → Nat, x: Nat): Nat = f (λ y: Nat = y);

// CHECK-DAG: extern lam f (x_{{[0-9]+}}: Nat): Nat =
// CHECK-DAG: f_{{[0-9]+}}: (Nat → Nat) → Nat
