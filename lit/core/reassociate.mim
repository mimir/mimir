// RUN: rm -f %t.ll
// RUN: %mim -p opt %s -o - | %FileCheck %s

plugin core;

use core.ops.u.w;

extern lam add(w: I32): I32 =
    let xy = 23I32;
    let z  = 42I32;
    xy + (z + w);

// CHECK-DAG: 65
