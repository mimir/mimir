// RUN: %mim %s

plugin core;
plugin refly;

lam myand (a b: Bool): Bool =
    refly.struc.e (core.bit2.and_ 0 (core.icmp.uge (a, b), core.icmp.ug (a, b)),
                          core.icmp.ug (a, b));
