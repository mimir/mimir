// RUN: rm -f %t.ll
// RUN: %mim -p opt %s -o - | %FileCheck %s

plugin core;

extern con icmp_lit(return: Cn Bool) =
    return (core.icmp.e (core.icmp.uge (tt, ff), core.icmp.ug (tt, ff)));

// CHECK-DAG: return{{.*}}tt
